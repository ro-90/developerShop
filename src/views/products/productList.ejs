<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

  <body>
    <%- include('../partials/header') %>
      <main class="my-4">
        <section class="container">
          <div class="row row-gap-3">
            <% if (products.length) { %>
                <% products.forEach(({name, price, image, id}) => { %>
                <div class="col-12 col-md-6 col-lg-3">
                    <article class="card shadow">
                    <div class="card-body">
                        <div class="position-relative">
                        <img style="height: 200px; object-fit: contain;" src="/images/<%= image %>"
                            alt="remeraBlancaAngular" class="card-img-top">
                        <p class="position-absolute top-0 end-0 badge rounded-pill text-bg-success fs-5"> $<%= price %> </p>
                        </div>
                        <hr>
                        <p class="cart-title fs-4"><%= name %></p>
                        <a class="btn btn-sm btn-info" href="/products/detail/<%= id %>"><i class="fas fa-plus"></i> Más info</a>
                        <a class="btn btn-sm btn-primary" href="/cart/add/<%= id %> "><i class="fas fa-cart-plus"></i> Comprar</a>
                    </div>
                    </article>
                </div>
                <% }) %>
            <% } else { %>
                <div style="height: 55vh;">
                    <h3 class="alert alert-warning">No hay resultado para la búsqueda</h3>
                </div>
                
            <% } %>
          </div>    
        </section>
      </main>
      <%- include('../partials/footer') %>

  </body>

</html>