<!DOCTYPE html>
<html lang="en">
  
    
          
                <%- include('../partials/head') %> 
            
             <body>
                <%- include('../partials/header') %>
            <main><h2>Iniciar Sesión</h2>
                <div class="form-container">
               
                <form action="/loginController" method="get">
                    <label for="email">Correo Electrónico:</label>
                    <input type="email" id="email" name="email" required value="<%= locals.correo ? correo : ""%>" >
                    <label for="password">Contraseña:</label>
                    <input type="password" id="password" name="password" required value="<%= locals.contrasena ? contrasena : ""%>" >
                    <% if(locals.errores) {%>
                        <p> <i class="fa-solid fa-circle-exclamation"></i> Credenciales invalidas</p>
                    <% } %>
                    <label for="recuerdame">Desea que recordemos su sesion?</label><input type="checkbox" id="recuerdame" name="recuerdame" value="true">
                    <button type="submit">Entrar</button>
                </form></div>
            </main>
                <%- include('../partials/footer') %>
            </body>